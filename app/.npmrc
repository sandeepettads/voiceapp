# Use the same registry URL as found in the ORxClientPortal-UI-main project
registry=https://repo1.uhc.com/artifactory/api/npm/npm-virtual/
@optum-mcp:registry=https://repo1.uhc.com/artifactory/api/npm/npm-virtual/
@optum-rx-skyline:registry=https://repo1.uhc.com/artifactory/api/npm/npm-virtual/
always-auth=true

# You may need to be on the Optum VPN to access internal resources
# Uncomment if needed based on your network configuration
# proxy=http://proxy.uhc.com:8080
# https-proxy=http://proxy.uhc.com:8080
# strict-ssl=false
